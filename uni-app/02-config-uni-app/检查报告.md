# 02-config-uni-app 项目检查报告

> 检查时间: 2024 年 12 月 4 日  
> 原项目时间: 2022 年  
> 项目类型: uni-app 配置学习项目

---

## 📋 检查概述

这是一个学习 uni-app 配置的项目，主要涉及 pages.json 和 manifest.json 的配置。

---

## ⚠️ 建议优化内容

### 1. 缺少 easycom 自动引入配置

**文件**: `pages.json`

**当前状态**: 未配置 easycom

**问题说明**:

- 缺少组件自动引入配置
- 使用 uni-ui 组件时需要手动导入
- 增加了代码量和维护成本

**官方推荐**: 添加 easycom 配置以实现组件自动引入

**修改建议**:
在 `pages.json` 中添加以下配置：

```json
{
    "easycom": {
        "autoscan": true,
        "custom": {
            "^uni-(.*)": "@dcloudio/uni-ui/lib/uni-$1/uni-$1.vue"
        }
    },
    "pages": [...],
    "globalStyle": {...}
}
```

**优势**:

- ✅ 自动按需引入组件
- ✅ 减少手动 import 代码
- ✅ 优化打包体积
- ✅ 提升开发效率

**参考文档**: https://uniapp.dcloud.net.cn/collocation/pages.html#easycom

---

## ✅ 仍然有效的内容

### 1. globalStyle 配置

**文件**: `pages.json`

```json
"globalStyle": {
    "navigationBarTextStyle": "white",
    "navigationBarTitleText": "购物街",
    "navigationBarBackgroundColor": "#ff8198",
    "backgroundColor": "#F8F8F8"
}
```

✅ 配置方式正确，继续使用

**说明**:

- `navigationBarTextStyle`: 导航栏文字颜色 (black/white)
- `navigationBarTitleText`: 导航栏标题
- `navigationBarBackgroundColor`: 导航栏背景色
- `backgroundColor`: 页面背景色

---

### 2. pages 数组配置

```json
"pages": [
    {
        "path": "pages/index/index",
        "style": {
            "navigationBarTitleText": "uni-app"
        }
    }
]
```

✅ 配置结构正确

**说明**:

- pages 数组第一项为应用启动页
- 每个页面可以单独配置 style 覆盖 globalStyle

---

## 💡 扩展建议

### 1. 添加 tabBar 配置示例

如果项目需要底部导航栏，可以添加：

```json
"tabBar": {
    "color": "#666",
    "selectedColor": "#ff8198",
    "backgroundColor": "#fff",
    "borderStyle": "black",
    "list": [
        {
            "pagePath": "pages/home/home",
            "text": "首页",
            "iconPath": "static/tabbar/home.png",
            "selectedIconPath": "static/tabbar/home_active.png"
        },
        {
            "pagePath": "pages/profile/profile",
            "text": "我的",
            "iconPath": "static/tabbar/profile.png",
            "selectedIconPath": "static/tabbar/profile_active.png"
        }
    ]
}
```

---

### 2. 添加分包配置

对于大型项目，建议使用分包优化：

```json
"subPackages": [
    {
        "root": "pages/sub",
        "pages": [
            {
                "path": "detail/detail",
                "style": {
                    "navigationBarTitleText": "详情"
                }
            }
        ]
    }
]
```

**优势**:

- 优化首屏加载速度
- 减少主包体积
- 按需加载子包

---

### 3. 添加预加载配置

```json
"preloadRule": {
    "pages/home/home": {
        "network": "all",
        "packages": ["pages/sub"]
    }
}
```

---

## 🔧 需要执行的操作

### 建议优化 (中优先级 ⚠️)

1. ⚠️ 在 `pages.json` 中添加 easycom 配置
2. ⚠️ 根据项目需求考虑添加 tabBar 配置
3. ⚠️ 大型项目考虑使用分包配置

---

## 📚 相关文档

- [pages.json 配置](https://uniapp.dcloud.net.cn/collocation/pages.html)
- [easycom 自动引入](https://uniapp.dcloud.net.cn/collocation/pages.html#easycom)
- [tabBar 配置](https://uniapp.dcloud.net.cn/collocation/pages.html#tabbar)
- [分包加载](https://uniapp.dcloud.net.cn/collocation/pages.html#subpackages)

---

## 📝 配置模板参考

### 完整的 pages.json 配置模板

```json
{
    "easycom": {
        "autoscan": true,
        "custom": {
            "^uni-(.*)": "@dcloudio/uni-ui/lib/uni-$1/uni-$1.vue"
        }
    },
    "pages": [
        {
            "path": "pages/index/index",
            "style": {
                "navigationBarTitleText": "首页"
            }
        }
    ],
    "globalStyle": {
        "navigationBarTextStyle": "white",
        "navigationBarTitleText": "应用标题",
        "navigationBarBackgroundColor": "#ff8198",
        "backgroundColor": "#F8F8F8",
        "enablePullDownRefresh": false
    },
    "tabBar": {
        "color": "#666",
        "selectedColor": "#ff8198",
        "backgroundColor": "#fff",
        "list": [...]
    }
}
```

---

**检查完成** ✓
