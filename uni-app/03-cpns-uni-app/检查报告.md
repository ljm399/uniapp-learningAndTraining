# 03-cpns-uni-app 项目检查报告

> 检查时间: 2024 年 12 月 4 日  
> 原项目时间: 2022 年  
> 项目类型: uni-app 组件使用学习项目

---

## 📋 检查概述

这是一个学习 uni-app 基础组件的项目，包含 view、text、button、image、scroll-view、swiper 等组件的使用。

---

## ⚠️ 需要注意的内容

### 1. CSS 深度选择器兼容性问题

**文件**: `pages/index/index.vue` (第 105 行)

**问题代码**:

```scss
:deep(.hy-h-scroll .uni-scroll-view::-webkit-scrollbar) {
  display: none;
}
```

**问题说明**:

- `:deep()` 在某些平台可能不生效
- 小程序平台对深度选择器支持有限
- 可能导致样式在不同平台表现不一致

**官方推荐**: 使用 `:global()` 或条件编译处理不同平台

**修改建议**:

**方案 1: 使用 :global()**

```scss
:global(.uni-scroll-view::-webkit-scrollbar) {
  display: none;
}
```

**方案 2: 使用条件编译**

```scss
/* #ifdef H5 */
:deep(.hy-h-scroll .uni-scroll-view::-webkit-scrollbar) {
  display: none;
}
/* #endif */

/* #ifdef MP-WEIXIN */
.hy-h-scroll ::-webkit-scrollbar {
  display: none;
}
/* #endif */
```

**参考文档**: https://uniapp.dcloud.net.cn/tutorial/vue3-api.html#css

---

### 2. 建议使用 Vue 3 Composition API

**文件**: `pages/index/index.vue` (第 62-77 行)

**当前代码**:

```javascript
export default {
  data() {
    return {
      title: "Hello",
      cvy,
    };
  },
  onLoad() {},
  methods: {},
};
```

**问题说明**:

- 使用的是 Vue 2 Options API 写法
- Vue 3 推荐使用 Composition API
- 代码组织性和复用性较差

**官方推荐**: 使用 `<script setup>` 语法

**修改建议**:

```vue
<script setup>
import { ref } from "vue";
import { onLoad } from "@dcloudio/uni-app";
import cvy from "@/static/images/cvy.png";

const title = ref("Hello");

onLoad(() => {
  console.log("页面加载");
});
</script>
```

**优势**:

- ✅ 更简洁的代码
- ✅ 更好的 TypeScript 支持
- ✅ 更好的性能
- ✅ 更容易复用逻辑

---

## ✅ 仍然有效的内容

### 1. 基础组件使用

```vue
<view class="content">
    <text class="text">我是一个Text组件</text>
    <button type="primary">我是一个button</button>
    <image :src="cvy" mode="widthFix"></image>
</view>
```

✅ 组件使用方式正确

**说明**:

- `view`: 视图容器，类似 div
- `text`: 文本组件
- `button`: 按钮组件
- `image`: 图片组件

---

### 2. scroll-view 组件

```vue
<!-- 垂直滚动 -->
<scroll-view scroll-y="true" class="hy-v-scroll">
    <view class="v-item">item1</view>
</scroll-view>

<!-- 水平滚动 -->
<scroll-view scroll-x="true" class="hy-h-scroll" :show-scrollbar="false">
    <view class="h-item">item1</view>
</scroll-view>
```

✅ 使用方式正确

**属性说明**:

- `scroll-y`: 允许纵向滚动
- `scroll-x`: 允许横向滚动
- `show-scrollbar`: 是否显示滚动条

---

### 3. swiper 轮播组件

```vue
<swiper
  class="hy-swiper"
  :indicator-dots="true"
  indicator-active-color="#ff8198"
  indicator-color="#f8f8f8"
  :autoplay="true"
  :interval="3000"
  :duration="1000"
>
    <swiper-item>
        <image src="@/static/images/banner/banner01.jpeg"></image>
    </swiper-item>
</swiper>
```

✅ 配置正确

**属性说明**:

- `indicator-dots`: 是否显示指示点
- `autoplay`: 是否自动切换
- `interval`: 自动切换时间间隔
- `duration`: 滑动动画时长

---

### 4. rpx 单位使用

```scss
.image {
  width: 200rpx;
}
.hy-v-scroll {
  height: 400rpx;
}
```

✅ rpx 响应式单位使用正确

**说明**:

- rpx 是 uni-app 的响应式单位
- 750rpx = 屏幕宽度
- 自动适配不同屏幕尺寸

---

## 💡 优化建议

### 1. 图片路径优化

**当前方式**:

```vue
<script>
import cvy from "@/static/images/cvy.png";
export default {
  data() {
    return { cvy };
  },
};
</script>
```

**推荐方式**:

```vue
<script setup>
import cvy from "@/static/images/cvy.png";
</script>

<template>
  <image :src="cvy" mode="widthFix"></image>
</template>
```

---

### 2. 使用 uni-ui 组件库

对于复杂的滚动场景，可以考虑使用 uni-ui 的组件：

```bash
npm install @dcloudio/uni-ui
```

```vue
<uni-list>
    <uni-list-item title="标题1"></uni-list-item>
    <uni-list-item title="标题2"></uni-list-item>
</uni-list>
```

---

## 🔧 需要执行的操作

### 建议优化 (中优先级 ⚠️)

1. ⚠️ 修改 `:deep()` 为 `:global()` 或使用条件编译
2. ⚠️ 将 Options API 改为 Composition API (`<script setup>`)
3. ⚠️ 考虑使用 uni-ui 组件库提升开发效率

---

## 📚 相关文档

- [uni-app 组件](https://uniapp.dcloud.net.cn/component/)
- [view 组件](https://uniapp.dcloud.net.cn/component/view.html)
- [scroll-view 组件](https://uniapp.dcloud.net.cn/component/scroll-view.html)
- [swiper 组件](https://uniapp.dcloud.net.cn/component/swiper.html)
- [Vue 3 Composition API](https://uniapp.dcloud.net.cn/tutorial/vue3-api.html)
- [条件编译](https://uniapp.dcloud.net.cn/tutorial/platform.html)

---

**检查完成** ✓
