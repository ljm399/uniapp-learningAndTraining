# 10-pages-uni-app 项目检查报告

> 检查时间: 2024 年 12 月 4 日  
> 原项目时间: 2022 年  
> 项目类型: uni-app 页面路由学习项目

---

## 📋 检查概述

这是一个学习 uni-app 页面路由和 tabBar 配置的项目。

---

## ⚠️ 建议优化内容

### 1. tabBar 图标建议优化

**文件**: `pages.json` (第 64-93 行)

**当前配置**:

```json
"tabBar": {
    "list": [
        {
            "iconPath": "static/images/tabbar/home.png",
            "selectedIconPath": "static/images/tabbar/home_active.png"
        }
    ]
}
```

**问题说明**:

- 使用图片作为 tabBar 图标
- 增加包体积
- 不支持动态颜色
- 不同分辨率显示效果可能不一致

**官方推荐**: 使用 iconfont 或 uni-icons

**优化方案**:

**方案 1: 使用 iconfont**

```json
"tabBar": {
    "iconfontSrc": "static/iconfont.ttf",
    "list": [
        {
            "text": "首页",
            "pagePath": "pages/home/home",
            "iconfont": {
                "text": "\\ue100",
                "selectedText": "\\ue101",
                "fontSize": "22px",
                "color": "#666",
                "selectedColor": "#ff8198"
            }
        }
    ]
}
```

**方案 2: 使用自定义 tabBar**

```javascript
// custom-tab-bar/index.vue
<template>
    <view class="tab-bar">
        <view
            v-for="(item, index) in list"
            :key="index"
            @click="switchTab(item.pagePath)">
            <uni-icons
                :type="item.iconType"
                :color="currentIndex === index ? '#ff8198' : '#666'">
            </uni-icons>
            <text>{{ item.text }}</text>
        </view>
    </view>
</template>
```

**优势**:

- ✅ 减小包体积
- ✅ 支持动态颜色
- ✅ 更清晰的显示效果
- ✅ 易于维护

**参考文档**: https://uniapp.dcloud.net.cn/collocation/pages.html#tabbar

---

## ✅ 仍然有效的内容

### 1. pages 数组配置

```json
"pages": [
    {
        "path": "pages/home/home",
        "style": {
            "navigationBarTitleText": "uni-app"
        }
    },
    {
        "path": "pages/category/category"
    },
    {
        "path": "pages/cart/cart"
    },
    {
        "path": "pages/profile/profile"
    }
]
```

✅ 配置方式正确

**说明**:

- pages 数组第一项为应用启动页
- 每个页面可以单独配置 style

---

### 2. tabBar 基本配置

```json
"tabBar": {
    "color": "#666",
    "selectedColor": "#ff8198",
    "list": [...]
}
```

✅ 基本配置正确

**属性说明**:

- `color`: 未选中时的文字颜色
- `selectedColor`: 选中时的文字颜色
- `backgroundColor`: tabBar 背景色
- `borderStyle`: tabBar 边框颜色

---

### 3. 页面 style 配置

```json
{
  "path": "pages/detail04/detail04",
  "style": {
    "navigationBarTitleText": "",
    "enablePullDownRefresh": true
  }
}
```

✅ 页面级配置正确

**常用属性**:

- `navigationBarTitleText`: 导航栏标题
- `enablePullDownRefresh`: 是否开启下拉刷新
- `navigationBarBackgroundColor`: 导航栏背景色
- `navigationBarTextStyle`: 导航栏文字颜色

---

## 💡 扩展建议

### 1. 使用分包加载

对于页面较多的项目，建议使用分包：

```json
{
  "pages": [
    {
      "path": "pages/home/home"
    }
  ],
  "subPackages": [
    {
      "root": "pages/sub",
      "pages": [
        {
          "path": "detail01/detail01",
          "style": {
            "navigationBarTitleText": "详情1"
          }
        },
        {
          "path": "detail02/detail02"
        }
      ]
    }
  ]
}
```

**优势**:

- 优化首屏加载速度
- 减少主包体积
- 按需加载子包

---

### 2. 配置预加载规则

```json
{
  "preloadRule": {
    "pages/home/home": {
      "network": "all",
      "packages": ["pages/sub"]
    }
  }
}
```

**说明**:

- 在进入某个页面时，预加载分包
- 提升用户体验

---

### 3. 自定义 tabBar

```json
{
    "tabBar": {
        "custom": true,
        "list": [...]
    }
}
```

然后创建 `custom-tab-bar` 组件：

```vue
<template>
  <view class="custom-tab-bar">
    <view
      v-for="(item, index) in tabList"
      :key="index"
      class="tab-item"
      :class="{ active: currentIndex === index }"
      @click="switchTab(item, index)"
    >
      <uni-icons
        :type="item.icon"
        :size="24"
        :color="currentIndex === index ? '#ff8198' : '#666'"
      >
      </uni-icons>
      <text>{{ item.text }}</text>
    </view>
  </view>
</template>

<script setup>
import { ref } from "vue";

const currentIndex = ref(0);
const tabList = [
  { text: "首页", icon: "home", pagePath: "/pages/home/home" },
  { text: "分类", icon: "bars", pagePath: "/pages/category/category" },
  { text: "购物车", icon: "cart", pagePath: "/pages/cart/cart" },
  { text: "我的", icon: "contact", pagePath: "/pages/profile/profile" },
];

const switchTab = (item, index) => {
  currentIndex.value = index;
  uni.switchTab({
    url: item.pagePath,
  });
};
</script>
```

---

## 🔧 需要执行的操作

### 建议优化 (中优先级 ⚠️)

1. ⚠️ 考虑使用 iconfont 替代图片图标
2. ⚠️ 对于大型项目，使用分包加载
3. ⚠️ 配置预加载规则提升体验
4. ⚠️ 考虑使用自定义 tabBar 获得更多自由度

---

## 📚 相关文档

- [pages.json 配置](https://uniapp.dcloud.net.cn/collocation/pages.html)
- [tabBar 配置](https://uniapp.dcloud.net.cn/collocation/pages.html#tabbar)
- [分包加载](https://uniapp.dcloud.net.cn/collocation/pages.html#subpackages)
- [自定义 tabBar](https://uniapp.dcloud.net.cn/api/ui/tabbar.html#custom-tab-bar)

---

## 📝 完整配置示例

```json
{
  "easycom": {
    "autoscan": true,
    "custom": {
      "^uni-(.*)": "@dcloudio/uni-ui/lib/uni-$1/uni-$1.vue"
    }
  },
  "pages": [
    {
      "path": "pages/home/home",
      "style": {
        "navigationBarTitleText": "首页"
      }
    },
    {
      "path": "pages/category/category",
      "style": {
        "navigationBarTitleText": "分类"
      }
    },
    {
      "path": "pages/cart/cart",
      "style": {
        "navigationBarTitleText": "购物车"
      }
    },
    {
      "path": "pages/profile/profile",
      "style": {
        "navigationBarTitleText": "我的"
      }
    }
  ],
  "subPackages": [
    {
      "root": "pages/sub",
      "pages": [
        {
          "path": "detail/detail",
          "style": {
            "navigationBarTitleText": "详情"
          }
        }
      ]
    }
  ],
  "preloadRule": {
    "pages/home/home": {
      "network": "all",
      "packages": ["pages/sub"]
    }
  },
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "uni-app",
    "navigationBarBackgroundColor": "#F8F8F8",
    "backgroundColor": "#F8F8F8"
  },
  "tabBar": {
    "color": "#666",
    "selectedColor": "#ff8198",
    "backgroundColor": "#fff",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/home/home",
        "text": "首页",
        "iconPath": "static/tabbar/home.png",
        "selectedIconPath": "static/tabbar/home_active.png"
      },
      {
        "pagePath": "pages/category/category",
        "text": "分类",
        "iconPath": "static/tabbar/category.png",
        "selectedIconPath": "static/tabbar/category_active.png"
      },
      {
        "pagePath": "pages/cart/cart",
        "text": "购物车",
        "iconPath": "static/tabbar/cart.png",
        "selectedIconPath": "static/tabbar/cart_active.png"
      },
      {
        "pagePath": "pages/profile/profile",
        "text": "我的",
        "iconPath": "static/tabbar/profile.png",
        "selectedIconPath": "static/tabbar/profile_active.png"
      }
    ]
  }
}
```

---

**检查完成** ✓
